streamlit>=1.42          # st.login / st.user need 1.42+
pandas>=2.0
altair>=5.0
requests>=2.31
feedparser>=6.0
praw>=7.7
python-dotenv>=1.0
anthropic>=0.40
Authlib>=1.3.2           # OIDC handshake behind st.login
psycopg[binary]>=3.1        # Supabase (Postgres) durable store
